# Individual instrument weights — Step 2 of 2
# ─────────────────────────────────────────────────────────────────
# Derived from group_weights. Edit individual weights if needed.
# Must sum to 1.0 (tolerance ±0.005).
#
instrument_weights:
  # Group: crypto (group weight: 0.20 → 0.1000 per instrument)
  BTC: 0.1  # crypto: 0.20 / 2
  ETH: 0.1
  # Group: index (group weight: 0.45 → 0.1500 per instrument)
  US500: 0.15  # index: 0.45 / 3
  US30: 0.15
  GER40: 0.15
  # Group: commodity (group weight: 0.25 → 0.1250 per instrument)
  XAU: 0.125  # commodity: 0.25 / 2
  XAG: 0.125
  # Group: currency (group weight: 0.10 → 0.1000 per instrument)
  USDJPY: 0.1  # currency: 0.10 / 1
